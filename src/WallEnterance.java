public class WallEnterance {
	 double length;
	 int texture;
	 double correction;
	 double dir;
	 WallEnterance(double length,  double correction,double dir,int texture){
		 this.length= length;
		 this.texture = texture;
		 this.correction = correction;
		 this.dir=dir;
		 
	 }
	 
}
